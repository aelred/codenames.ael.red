server {
    listen 80;
    server_name {{ inventory_hostname_short }}.*;

    root /home/{{ ansible_ssh_user }}/sites/{{ inventory_hostname_short }}/static;

    location / {
        autoindex on;
    }
}
